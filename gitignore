# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Editor directories and files
.idea/
.vscode/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.sublime-project
*.sublime-workspace

# Node (if a local dev server is used)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm

# Python (if used for local server)
__pycache__/
*.py[cod]
*.pyo

# Temporary files
*.tmp
*.temp
*.bak
*.orig

# Build artifacts (if any build step is introduced later)
dist/
build/
.cache/

# Environment files
.env
.env.local
.env.*.local

# Log files
*.log
logs/

# Archive files that shouldn't be in version control
*.zip
*.tar.gz
*.rar

# Large media files (add to Git LFS instead if needed)
# *.mp4
# *.mov
